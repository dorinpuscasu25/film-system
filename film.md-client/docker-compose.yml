services:
  storefront:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api/v1}
    container_name: film-md-storefront
    ports:
      - "${CLIENT_FRONTEND_PORT:-4173}:80"
    restart: unless-stopped
